#include "main.h"

/**
 * mul - entry point
 *
 * description -
 *
 * @a: number to multiply
 * @b: number to multiply
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int r;

	r = a * b;
       return (r);
}
