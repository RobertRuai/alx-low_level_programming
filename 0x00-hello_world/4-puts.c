/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
/*this code uses puts function instead of printf*/
#include <stdio.h>
#include <string.h>

int main(void)
{
char str1[] = "\"Programming is like building a multilingual puzzle\n";
puts(str1);
return (0);
}
